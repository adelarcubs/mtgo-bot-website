<!DOCTYPE html>
<html lang="{{ current_locale|slice(0, 2) }}">
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <title>{% block title %}Admin{% if block('title') is defined and block('title') is not empty %} | {#{ parent() }#}{% endif %}{% endblock %} - Braz Bot</title>
    <link rel="shortcut icon" href="https://getlaminas.org/images/favicon/favicon.ico" />
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" />
    <link href="https://use.fontawesome.com/releases/v5.13.0/css/all.css" rel="stylesheet" />
    <style>
        body { padding-top: 70px; }
        .app { min-height: 100vh; }
        .app-footer { padding: 2em 0; }
        .admin-sidebar {
            min-height: calc(100vh - 70px);
            background-color: #f8f9fa;
            padding: 20px 0;
            border-right: 1px solid #dee2e6;
        }
        .admin-content {
            padding: 20px;
        }
        .navbar-admin {
            background-color: #343a40 !important;
        }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body class="app">
    <header class="app-header">
        {% include '@layout/menu/admin-top-menu.html.twig' %}
    </header>

    <div class="container-fluid">
        <div class="row">
            <nav class="col-md-2 d-none d-md-block admin-sidebar">
                {% include '@layout/menu/admin-sidebar-menu.html.twig' %}
            </nav>
            <main role="main" class="col-md-10 ml-sm-auto admin-content">
                {#% include 'partials/flash-messages.html.twig' %#}
                {% block content %}{% endblock %}
            </main>
        </div>
    </div>

    <footer class="app-footer bg-dark text-white mt-4">
        <div class="container">
            <div class="row">
                <div class="col-12 text-center">
                    <p class="mb-0">
                        &copy; {{ "now"|date("Y") }} Braz Bot Admin Panel
                    </p>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
    {% block javascript %}{% endblock %}
</body>
</html>
